name: Deploy to Cloud Run

env:
  SERVICE_NAME: learners-digital-website
  PROJECT_ID: learners-digital-480411
  REGION: us-central1

on:
  push:
    branches:
      - main

jobs:
  dockerize-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Google Cloud Auth
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'
          project_id: ${{ env.PROJECT_ID }}

      - name: Set up Cloud SDK
        uses: 'google-github-actions/setup-gcloud@v2'

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker us-docker.pkg.dev

      - name: Build and Push Docker Image
        run: |
          TAG=${{ github.sha::0:7 }}
          docker build -t us-docker.pkg.dev/learners-digital-480411/learners-digital-docker-repo/learners-digital-website:$TAG .
          docker push us-docker.pkg.dev/learners-digital-480411/learners-digital-docker-repo/learners-digital-website:$TAG

      - name: Deploy to Cloud Run
        run: |
          TAG=${{ github.sha::0:7 }}
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image us-docker.pkg.dev/learners-digital-480411/learners-digital-docker-repo/learners-digital-website:$TAG \
            --platform managed \
            --region ${{ env.REGION }} \
            --allow-unauthenticated \
            --port 8080 \
            --memory 512Mi
